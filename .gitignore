# Python
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/

# uv-managed virtualenv (uv.lock IS committed)
.venv/

# Local data — regenerable via `lockin ingest`
*.db
*.db-wal
*.db-shm
data/
logs/

# Rendered output — regenerable via `lockin dashboard`, and it embeds the
# league's manager names, which do not belong in the repo.
dashboard.html

# Claude Code local settings and harness temp files
.claude/settings.local.json
.claude/*.tmp.*
